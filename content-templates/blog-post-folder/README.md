# Blog post folder template

Copy this folder to create a new multilingual blog post:

```bash
cp -R content-templates/blog-post-folder docs/blog/posts/YYYY-slug
```

Then edit `index.md`, `index.es.md`, and `index.pt.md`.
